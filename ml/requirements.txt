# RiskShield AI - ML component dependencies
# Install with: pip install -r ml/requirements.txt --break-system-packages
# (the --break-system-packages flag is only needed on systems like Ubuntu 24.04
#  that protect the system Python; use a virtualenv instead if you prefer)

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
